import { LucideIcon } from "lucide-react";

export interface QuickLinkItem {
  title: string;
  description: string;
  icon: LucideIcon;
  path: string;
  color: string;
  buttonText: string;
  image: string;
}